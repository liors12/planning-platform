fastapi>=0.110
uvicorn[standard]>=0.27
sqlalchemy>=2.0
pydantic>=2.6
sqlcipher3>=0.5
python-multipart
# openpyxl is used by compliance_engine.excel_export for the
# "הפיקי אקסל" workbook export. Lazy-imported (function-body), so
# PyInstaller also needs collect_submodules("openpyxl") in backend.spec.
openpyxl>=3.1
# PyMuPDF (import fitz) — PDF text extraction for referent-comments ingestion.
# Already listed as a hidden import in backend.spec for PyInstaller.
PyMuPDF>=1.24
# DXF geometry analysis (Phase 1 CAD checks).
# collect_submodules/collect_data_files in backend.spec depend on these being
# installed at PyInstaller analysis time — missing entries here cause CI to fail.
ezdxf>=0.18
shapely>=2.0
# pdfplumber — top-level import in compliance_engine/format_rules_checker.py.
# NOT being here means PyInstaller can't collect it → No module named 'pdfplumber'
# in the frozen bundle. pdfminer.six is pulled in transitively by pdfplumber.
pdfplumber>=0.10
# google-generativeai — lazy-imported (try/except) in submission_data_extractor,
# meeting_extract, email_extract, referent_extract, vision_verify. Not in CI before
# this means collect_submodules("google.generativeai") in backend.spec was a no-op
# and those paths fell back silently. Must be installed for PyInstaller to collect it.
google-generativeai>=0.5
