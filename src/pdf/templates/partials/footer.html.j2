{# Footer disclaimer — appears once at the end of document body.
   The page-number running footer is applied via @page rules in CSS. #}
<footer class="opinion-footer">
  <p class="disclaimer">
    מסמך זה הופק על ידי מערכת בדיקת תאימות אוטומטית בתאריך
    <span class="generated-date">{{ run.generated_at }}</span>.
    נדרש אישור מהנדס מורשה לפני הפיכת המסמך לחוות דעת תקפה.
  </p>
</footer>
