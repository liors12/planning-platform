{
  "name": "planning-platform-e2e-ui",
  "private": true,
  "version": "0.1.0",
  "description": "Real-WebView2 UI smoke test for the packaged Tauri app. Drives the installed Planning Platform Windows binary via CDP and asserts the user-visible flow Ellen runs. See README.md for design notes.",
  "scripts": {
    "test": "playwright test"
  },
  "devDependencies": {
    "@playwright/test": "^1.49.0",
    "@types/node": "^20.17.0",
    "typescript": "^5.6.3"
  }
}
